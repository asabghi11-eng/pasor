fastapi
uvicorn[standard]
websockets
requests
google-auth
PyJWT
psycopg2-binary  # only needed if you set DATABASE_URL (PostgreSQL); SQLite needs nothing extra
redis  # only needed if you set REDIS_URL (multi-instance realtime); see hokm_realtime.py